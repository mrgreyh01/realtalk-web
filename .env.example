# Origin of the chat backend. No trailing slash.
# The browser calls this directly for REST and for the Socket.IO handshake,
# so it must be reachable from the client, not only from the Next server.
NEXT_PUBLIC_API_URL=http://localhost:4000
